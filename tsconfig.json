{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
  ],
  "compilerOptions": {
    "composite": true, // Habilita a construção incremental
    "baseUrl": ".",    // Define a baseUrl para resolver módulos
    "paths": {
      "@/*": ["./src/*"] // Permite importar arquivos usando @/ como base para a pasta src
    }
  }
}
